"""AutoTmux — terminal dashboard for tmux sessions across slurm nodes.

The single source of truth for the package version lives here; both the
`atmux` frontend and the `atd` daemon import it, and pyproject reads it via
`[tool.setuptools.dynamic]`.
"""

__version__ = "0.6.0"
